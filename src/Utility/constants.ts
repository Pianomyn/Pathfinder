import { AllColorMapping } from "./types";

export const DEFAULT_HEIGHT = 20;
export const DEFAULT_WIDTH = 50;

export const VISITABLE_CELL_TYPES = [
  AllColorMapping.Unvisited,
  AllColorMapping.Weight,
  AllColorMapping.Target,
];
